import path from 'node:path';

export const PRODUCT_GROUPS_FILE = path.join(
  __dirname,
  '..',
  '..',
  'db',
  'product-groups.json'
);

export const PRODUCT_TYPES_FILE = path.join(
  __dirname,
  '..',
  '..',
  'db',
  'product-types.json'
);

export const PRODUCTS_FILE = path.join(
  __dirname,
  '..',
  '..',
  'db',
  'products.json'
);

export const PRODUCT_RESTRICTIONS_FILE = path.join(
  __dirname,
  '..',
  '..',
  'db',
  'product-restrictions.json'
);
